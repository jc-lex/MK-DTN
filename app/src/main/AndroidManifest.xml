<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.radindustries.radwolfsdragon.examples.wifip2ppeerdiscoverytest">

    <!--<uses-permission-->
        <!--android:name="android.permission.ACCESS_WIFI_STATE"-->
        <!--android:required="true" />-->
    <!--<uses-permission-->
        <!--android:name="android.permission.CHANGE_WIFI_STATE"-->
        <!--android:required="true" />-->
    <!--<uses-permission-->
        <!--android:name="android.permission.INTERNET"-->
        <!--android:required="true" />-->

    <!--<uses-sdk android:minSdkVersion="14" />-->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" android:required="true"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" android:required="true"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" android:required="true"/>
    <uses-permission android:name="android.permission.INTERNET" android:required="true"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" android:required="true"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:enabled="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--<service-->
            <!--android:name=".DoPeerDiscoveryService"-->
            <!--android:enabled="true"-->
            <!--android:exported="false" />-->

        <!-- Only those who can change the WIFI state can broadcast to this receiver -->
        <!-- Only listens to WIFI P2P events -->
        <!-- NB: actions got from WifiP2pManager docs & code -->
        <receiver
            android:name=".MyPeerDiscoveryBroadcastReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.CHANGE_WIFI_STATE">
            <intent-filter>
                <action android:name="android.net.wifi.p2p.STATE_CHANGED" />
                <action android:name="android.net.wifi.p2p.CONNECTION_STATE_CHANGE" />
                <action android:name="android.net.wifi.p2p.PEERS_CHANGED" />
                <action android:name="android.net.wifi.p2p.THIS_DEVICE_CHANGED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>